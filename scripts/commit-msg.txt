docs: architecture, requirements traceability, database design, CI

- docs/architecture.md: Mermaid system architecture, AI-generation sequence
  flow, logical ERD, deployment topology, security posture, file layout
- docs/requirements-matrix.md: every core SRS requirement mapped to feature,
  backend app, frontend, database, endpoint and tests (optional enhancements
  documented)
- docs/database.md: MongoDB collection-by-collection design with field tables
  and index strategy
- .github/workflows/ci.yml: backend (Mongo service, deps, system check, 112
  tests, OpenAPI generation check) + frontend (install, lint, build)
- Live Swagger UI wired at /api/v1/schema/ and /schema/swagger-ui/
- README expanded: features, demo mode walkthrough, testing, deployment,
  security, documentation index
- Final verification: 112 backend tests green, frontend build clean, Swagger
  schema served 200 via the Vite proxy