feat: Phase 11-13 - itinerary studio, sharing and exports

- Regenerate-Day engine: six mood presets (relaxed/balanced/packed/
  budget/premium/family) with AI path when configured + deterministic
  demo fallback
- Itinerary studio API: replace day activities, append/remove activity,
  regenerate day - every mutation re-repairs schedule and re-scores trip;
  manual trips auto-provision day slots (with dates) up to duration_days
- Sharing: token links (create/revoke), anonymous public view with private
  notes stripped; trips flip public/private with link lifecycle
- Exports: branded ReportLab PDF (header band, summary table, per-day
  activity grids, estimate disclaimer) and ICS calendar with proper
  character escaping; both owner-scoped and export-rate-limited
- Exception handler hardened for nested serializer error dicts
- Frontend: TripDetailPage rebuilt into itinerary studio (day tabs, mood
  regeneration, add/remove activities, share-link banner, PDF/ICS downloads
  via authenticated blob fetch); public /shared/:token read-only page;
  ActivityForm component
- 19 new backend tests (studio CRUD/regeneration, sharing privacy,
  exports); full suite at 112 passing; live smoke verified all flows
