feat: Phase 14 - admin console, famous spots, chat persistence, UI polish

- Admin panel (staff-only): /api/v1/admin/ stats (users/trips/conversations/
  AI generations + ai_enabled flag), user management (activate/deactivate/
  promote/delete with self-protection), trip management with owner emails
- seed_demo management command: creates admin@wandersync.test / Admin@12345
  and demo@wandersync.test / Demo@12345 (idempotent)
- Famous Spots catalog: curated countries -> iconic landmarks (8 countries,
  coordinates) at GET /api/v1/spots/ ; frontend page with flags, landmark
  descriptions, LIVE weather per spot, and "Ask the planner" deep-link (?q=)
- Planner chat history now persists across visits (localStorage conversation
  id + reload), New-chat button, Live-AI/Demo engine badge per reply,
  ?q= prefill auto-send
- Login page demo credentials box with one-click fill
- Page transition animations (AnimatePresence) in both layouts; card hover
  lifts across dashboard/admin/spots
- Fixed broken openai SDK install (httpx/distro/tqdm were missing from the
  interrupted pip runs); fixed Mongo $type operator collision in admin stats;
  hardened nested validation-error flattening
